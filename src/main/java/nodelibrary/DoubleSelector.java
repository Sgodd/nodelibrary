package nodelibrary;

import nodelibrary.editor.node.Node;
import nodelibrary.editor.node.components.Selector;

public class DoubleSelector extends Selector<Double> {

    public DoubleSelector(Node parent) {
        super(parent);
    }
    
}
